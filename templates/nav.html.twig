<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarNav"
    aria-controls="navbarNav"
    aria-expanded="false"
    aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
        <li class="nav-item {{ app.request.get('_route') == 'home' ? 'active' }}">
            <a class="nav-link" href="{{ path('home') }}">Accueil</span></a>
        </li>
        <li class="nav-item {{ app.request.get('_route') == 'offres_list' ? 'active' }}">
            <a class="nav-link" href="{{ path('offres_list') }}">Offres</a>
        </li>
    </ul>
    </div>
</nav>